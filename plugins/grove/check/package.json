{
  "name": "grove-review-bookkeeping-check",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Layer A review-bookkeeping check core (spec-0002). Pure functions over explicit inputs; no network. Wave 2b wires the GitHub/CI shell.",
  "scripts": {
    "test": "node --test",
    "typecheck": "node --check lib/*.mjs && node --check test/*.mjs"
  }
}
